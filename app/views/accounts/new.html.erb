<h1>Add Account</h1>
<%= form_with(model: @account, local: true) do |f| %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, required: true, placeholder: "Name" %>
  </div>
 
  <%= f.submit %>
<% end %>
